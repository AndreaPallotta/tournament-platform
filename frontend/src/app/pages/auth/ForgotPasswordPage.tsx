import { Form } from 'react-router-dom';

/**
 * UI component for the forgot password page.
 */
export default function ForgotPasswordPage() {
    /*
        To-do:
            - allow user to provide email
    */
}
