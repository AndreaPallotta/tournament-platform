export default function SettingsPage() {
    /*
        To-do:
            - allow at least users with the Player role to edit login information
            - allow at least users with the Player role to delete their account
    */
}